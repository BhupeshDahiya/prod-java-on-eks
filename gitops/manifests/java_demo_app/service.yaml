apiVersion: v1
kind: Service
metadata:
  name: java-demo-app-svc
  labels:
    app: java-demo-app
spec:
  selector:
     app: java-demo-app
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
